[package]
name = "contracts"
edition = "2024.beta"
license = "Apache-2.0"
authors = ["Inkray Team"]

[dependencies]
# Mainnet Walrus contracts (pulls mainnet WAL transitively). Sui framework resolves from the active mainnet env.
walrus = { git = "https://github.com/MystenLabs/walrus.git", subdir = "mainnet-contracts/walrus", rev = "mainnet" }
wal = { git = "https://github.com/MystenLabs/walrus.git", subdir = "mainnet-contracts/wal", rev = "mainnet" }
